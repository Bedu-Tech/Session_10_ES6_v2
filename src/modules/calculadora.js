let sumar = (x, y) => x + y
let restar = (x, y) => x - y
let multiplicar = (x, y) => x * y
let dividir = (x, y) => x / y
let residuo = (x, y) => x % y

export {
  sumar,
  restar,
  multiplicar,
  dividir,
  residuo
}